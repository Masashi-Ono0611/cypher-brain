node_modules/
# bun.lock is the only lockfile; a stray `npm install` must not add a second one
package-lock.json
*.age
work/
logs

# secrets — never commit a key. (Not a bare *.json: package.json etc. are tracked.)
*wallet*.json
*.jwk
ar-*-wallet.json
identity*
recipient.txt


# build output + local selftest logs
dist/
_baseline-logs/
_newlayout-logs/

# StrykerJS mutation-test scratch dir + generated HTML report (#228)
.stryker-tmp/
/reports/

# personal Claude Code settings + ephemeral agent worktree containers (not shipped code)
.claude/
